


// #include <stdio.h>

// int main() {
//     int dado, soma = 0, contador = 0;
//     float media;

//     printf("Digite os dados dos sensores (digite -1 para encerrar):\n");

//     do { // Validação para garantir que todos sejam diferentes de -1
//         scanf("%d", &dado);

//         if (dado != -1) { // Caso o valor digitado seja diferente de -1, ele entra na condição
//             soma += dado; // A soma inicia do 0, conforme sua declaração, e vai somando os valores digitados
//             contador++; // Contador para saber quantos valores foram digitados, e posteriormente, utiliza-lo para calculara média
//         }
//     } while (dado != -1); // Validação para garantir que todos sejam diferentes de -1

//     // Validação básica para garantir que não ocorra divisão por zero
//     if (contador > 0) {
//         media = (float)soma / contador;
//         printf("A média dos dados coletados é: %.2f\n", media);
//     } else {
//         printf("Nenhum dado válido foi coletado.\n");
//     }

//     return 0;
// }




